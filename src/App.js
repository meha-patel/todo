import "./styles.css";
import { useEffect, useState } from "react";
export default function App() {
  return (
    <div className="App">
      <h1>My ToDo</h1>
      <ul>
        {/* {number.map((num, id) => (
          <li key={id}>{num}</li>
        ))} */}
      </ul>
    </div>
  );
}
